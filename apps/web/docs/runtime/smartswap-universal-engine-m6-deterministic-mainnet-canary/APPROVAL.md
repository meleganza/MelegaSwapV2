# APPROVAL

Not broadcast.

Minimum WBNB `approve(executor, 10000000000000000)` is prepared in `deployments/mainnet/m6-unsigned-create-tx.json` for predicted executor `0x296015b106F4b2FB94249cf398cbF05d4CcE0391`.

Do not send until runtime certification and `setRouter` succeed. Rebuild if CREATE address changes. Not unlimited.
