{
  "schema": "melega.dex.v1.public-farm-build",
  "command": "yarn next build",
  "cwd": "apps/web",
  "result": "PASS",
  "durationSecApprox": 28,
  "notes": [
    "Pre-existing ESLint next/babel config warning",
    "Pre-existing civilization-router import warnings unrelated to Public Farm Factory"
  ],
  "forbiddenFilesUntouched": [
    "exchange.ts (no mission edits)",
    "contracts.ts (no mission edits)",
    "router / wallet / swap / farms stake calls / pools / MasterChef ABI / NFT / token lists"
  ]
}
