# Screenshots

Visual acceptance for this mission is encoded in static UI assertions (Instant tabs absent, Smart experience forced, Melega-only copy).

Verified surfaces:
- Home `#swap` terminal — single Swap title, no Instant|Smart tabs
- Trade cockpit — Melega liquidity subtitle + route preview
- Docs — Instant Swap section removed; Smart Swap is canonical

Live browser screenshots can be captured post-deploy against the mission branch build; no production deploy was performed by this mission.
