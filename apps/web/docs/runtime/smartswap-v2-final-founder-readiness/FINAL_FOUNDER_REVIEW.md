# SmartSwap V2 — Final Founder Review

Status: **READY_FOR_FOUNDER_DEPLOYMENT_REVIEW**  
Public path: **NOT GO_LIVE_READY**  
This package is report + current-main V2 compile artifacts only. No runtime/feature change. No merge, deploy, payment, approval, signature, broadcast, key handling, or activation was performed.

## BASE_MAIN_SHA

`65fa3dba016b66122d42eb31390322e059155bbe` (`origin/main` after #82)

Branch: `cursor/smartswap-v2-final-founder-readiness-c987`

#76 / #78 / #80 / #81 / #82 were not reopened. #50 was not reused. `/list` and Boost were not touched.

## ARTEFATTO_V2

Compiled **on this SHA** with the repo release Foundry profile. A prior `out-smartswap-executor-release` file is **not** the proof; this run was:

```bash
FOUNDRY_PROFILE=smartswap_executor_release forge build --force --no-lint
```

| Field | Value |
| --- | --- |
| Source | `contracts/smartswap/SmartSwapExecutorV2.sol` |
| Source commit | `65fa3dba016b66122d42eb31390322e059155bbe` |
| Source git blob | `eb5e54725a2d3cdc316de18fd542ac66dcfbf688` |
| Source SHA-256 | `e5a4acafa5114834d249b70758722ae91b5fc4a17b4776c0376f714440535b52` |
| Profile | `smartswap_executor_release` |
| solc | `0.8.20+commit.a1b79de6` |
| optimizer | enabled, 200 runs |
| viaIR | true |
| evmVersion | shanghai |
| metadata | `bytecodeHash=none`, `appendCBOR=false`, `useLiteralContent=true` |
| Creation bytecode SHA-256 | `36d2503e328425ab66b61e384fa02418ec18c29df3e7966f01ece0c1e4422217` |
| Deployed bytecode SHA-256 | `4ccb42b71a9a7826715f14a234a68c71dda90859a1e70c0981bb4a8695594722` |
| Creation keccak | `0xca5257e7de17873f4adeeaf38aafec86418fd5832b71c101f12681e748eff41c` |
| Deployed keccak | `0xa4c1719a1b08959730964998f27170ecfc7dbaee72738decad3bec825d199a87` |
| Creation size | 7070 bytes |
| Deployed size | 6602 bytes |
| Public executor address | **NON_DEPLOYED/UNVERIFIED** |

Package (does **not** overwrite V1):

- `deployments/smartswap-executor-v2/smart-swap-executor-v2-artifact.json`
- `deployments/smartswap-executor-v2/creation.hex`
- `deployments/smartswap-executor-v2/deployed.hex`
- `deployments/smartswap-executor-v2/not-executed-calls.json`

V1 guard (unchanged after this compile):

- `deployments/smartswap-executor-v1/smart-swap-executor-v1-artifact.json` SHA-256 `e653af3e1dde8dbfd27f6cdcbe10d0719b79f234fa2af092a00a4aca6118287e`
- `deployments/mainnet/smartswap-executor-v1-artifact.json` same hash

## TEST_ESEGUITI

| Command | Class | Count | Result |
| --- | --- | --- | --- |
| `FOUNDRY_PROFILE=smartswap_executor_release forge test --match-path 'test/smartswap/SmartSwapExecutorV2*.t.sol'` | **mock** Foundry (in-process `vm`, `MockSmartSwapV2Router` / mock ERC20 / MockWBNB) | 32 passed (28 + 4) | PASS |
| `vitest` `netInputQuoteEconomics.test.ts` | **mock** TS | 11 passed | PASS |
| `vitest` `melegaBscExactQuoteAndBinding.test.ts` | **mock** TS synthetic `getAmountsOut` + binding | 12 passed | PASS |
| `vitest` `v2ExecutionBinding.test.ts` | **mock** TS | 17 passed | PASS |
| `vitest` `v2TransactionPreparation.test.ts` unit cases | **mock** TS calldata encode/decode | included in 17 | PASS |
| `vitest` `v2TransactionPreparation.test.ts` `local EVM: userA Melega, userB Pancake...` | **local Anvil** execution of **TypeScript `prepareV2UserTransactions` calldata** | 1 local-EVM case inside the 17 | PASS |
| `vitest` `shadowRuntimePreflight.test.ts` (engine + preview) | **eth_call quotation** (mocked fetch) + helper isolation | 10 passed | PASS |

Totals actually executed: Foundry 32/32; Vitest 67/67.

### Proof-class rules applied

- **mock tests**: Foundry V2 suites and TS economics/binding/preflight fixtures.
- **local Anvil execution**: `v2TransactionPreparation` Anvil on `127.0.0.1:18556`, chain 56. Executor deployed from the **release-profile** artifact. Calldata generated by `prepareV2UserTransactions` (not a hand-built Solidity intent). Routers at certified addresses were **`anvil_setCode` of `MockSmartSwapV2Router`** — local execution against mock bytecode, not live AMM code.
- **fork with real routers**: **MISSING/BLOCKED** (see FORK_EVIDENCE). Not replaced by a quote or a mock.
- **mere eth_call quotation**: authorized SHADOW preflight / factual `getAmountsOut` tests. Quote only. Not execution.

## LOCAL_EVM / FORK_EVIDENCE

### LOCAL_EVM — present

Anvil proof in `v2TransactionPreparation.test.ts`:

- TS binding + `prepareV2UserTransactions` produces approve + `execute` calldata.
- Local executor constructor: Treasury `0xb6436EF4c7f76bE0f26c0C5C9dB72F2689abF65b`, WBNB, Anvil account 0 as owner (test-only).
- userA Melega and userB Pancake exact-in; fee/net/replay/rollback checked on the local executor.
- This is **not** a real-router proof.

### FORK_EVIDENCE — MISSING/BLOCKED

Exact cause (all of these):

1. No existing real-router fork harness for `SmartSwapExecutorV2.execute` + TS calldata.
2. `BNB_MAINNET_RPC_URL` **UNSET**. `ETHEREUM_RPC_URL` **UNSET**. No root `.env`.
3. Public Ankr ETH RPC (`https://rpc.ankr.com/eth`) returned `Unauthorized` (API key required).
4. Public BSC `eth_blockNumber` on `https://bsc-dataseed.binance.org` succeeded (`0x7518d95`). That is **JSON-RPC liveness only**, not a fork execute, not a real-router swap.

No quote, mock, or Anvil `setCode` result is labeled “real”.

## MATRICE_CHAIN_VENUE

Derived **only** from `certifiedVenues.ts` on this SHA. No extra chain or cross-chain venue.

| Chain | Venue | Catalog support | Router | Wrapped native | V2 execute today |
| --- | --- | --- | --- | --- | --- |
| BSC 56 | Melega V2 (`melega-dex`) | `SUPPORTED` | `0xc25033218D181b27D4a2944Fbb04FC055da4EAB3` | WBNB `0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c` | SHADOW quote / local mock only |
| BSC 56 | PancakeSwap V2 (`pancakeswap`) | `QUOTE_ONLY` | `0x10ED43C718714eb63d5aA57B78B54704E256024E` | same WBNB | SHADOW quote / local mock only |
| Ethereum 1 | Uniswap V2 (`uniswap`) | `QUOTE_ONLY` | `0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D` | WETH `0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2` | SHADOW quote / local mock only |

Not claimed: Pancake on ETH, Uniswap on BSC, Melega on ETH, Polygon, Base, Arbitrum, Avalanche, multi-DEX using another chain’s router.

`MELEGA_CHAIN_SUPPORT` is a separate M10 inventory table and is **not** used as the venue matrix here.

## PARAMETRI_DEPLOY

All constructor / `setRouter` values below are **NOT-EXECUTED data**.

Owner/deployer: `0xB6eEb3ab9695979F5b2Ef6Df4112e63212E33EE0`  
Treasury: `0xb6436EF4c7f76bE0f26c0C5C9dB72F2689abF65b`

`wrappedNative` is immutable, so BSC and Ethereum are **separate** constructor instances.

Fee bands unchanged (`SMARTSWAP_REVENUE_POLICY_V1` + on-chain `authorizedFeeBps`):

| Structural cost (bps) | Protocol fee (bps) | Catalog venues |
| --- | --- | --- |
| 0–10 | 25 | none in this matrix |
| 11–25 | 20 | Melega 25, Pancake 25 |
| 26–40 | 15 | Uniswap 30 |
| 41–60 | 10 | — |
| 61+ | 5 | — |
| cap | 25 | `MAX_PROTOCOL_FEE_BPS` |

Native-in fee path (code truth, `SmartSwapExecutorV2.execute`): `IWBNB(wrappedNative).deposit{value: feeAmount}()` then `IERC20(wrappedNative).safeTransfer(treasury, feeAmount)`. Treasury receives **wrapped native**, not raw ETH/BNB.

Intent `feeAsset` for native-in is the native sentinel `0xEeee…EeE`; the transfer asset is wrapped native.

## ROUTER_ALLOWLIST

On-chain `allowedVenue[router] = venueIdHash`. Unknown venue hashes revert `UnknownVenue`.

| Chain | Future `setRouter` (NOT-EXECUTED) | venueIdHash (`keccak256(string)`) |
| --- | --- | --- |
| 56 | Melega `0xc250…EAB3` → `melega-dex` | `0x877b20d585b9e74b8a131af0e7b570769017d6338d83cef67e371130e4125538` |
| 56 | Pancake `0x10ED…024E` → `pancakeswap` | `0xd7e0d5c07ddc27357df5c45737f3b7506ed8b6a6631c211732cdda1dfcf56ba3` |
| 1 | Uniswap `0x7a25…488D` → `uniswap` | `0xa2a03459171c76bff45817330c10ef9f8af07011a33005b73b50189bbc7e7132` |

Calldata is in `deployments/smartswap-executor-v2/not-executed-calls.json`. Nothing was sent.

## RUNTIME_ACTIVATION_GAPS

Code truth. **Not** “configuration ready”.

Already implemented:

- `SmartSwapExecutorV2` exact-in execute, owner `pause`/`unpause`/`setRouter`, no `intentSigner`.
- Net-input economics, Melega/Pancake/Uniswap SHADOW ranking, V2 binding, unsigned prep helper.
- SHADOW preflight attached internally as `shadowRuntime`. Modes: `LEGACY_PRODUCTION` / `SHADOW` / `isProductionCutoverAllowed() === false` (hardcoded).

Still required before any public V2 swap (missing link, not a flag flip):

1. Public `executorAddress` does not exist (`NON_DEPLOYED/UNVERIFIED`).
2. `useSwapCallback` still builds **legacy** `prepareMelegaSmartRouterSwap` calls. It does not import or call `prepareV2UserTransactions`.
3. `SmartSwapCommitButton` / `useSmartSwapExecution` still submit the legacy smart-router plan. SHADOW winner is not the CTA target.
4. Inserting an `executorAddress` into config **alone is not enough**. The CTA → unsigned V2 txs → wallet path is not connected.
5. Production flags are constants, not an env switch. Changing them is out of this package and would still leave the CTA on legacy.

This mission did **not** change flags, CTA, `useSwapCallback`, or contracts.

## CANARY / ROLLBACK

Canary:

- `uniswapEthereumCanaryPrep.ts` is a **V1-constructor** canary package (includes `intentSigner`). It is not a V2 public-path activation.
- No certified V2 canary pair/notional is selected in this package.
- V2 canary, if later authorized, would still require a deployed executor, `setRouter`, and a **non-public** execute path. Not available today.

Rollback / stay on legacy (already the live path):

- Do nothing: production execution is already `LEGACY_PRODUCTION`; cutover is already `false`.
- After a future deploy (not done here): owner `pause()` blocks `execute`; `setRouter(router, venueId, false)` clears allowlist; users keep using the existing Swap CTA → legacy router.
- Returning to legacy does **not** require flipping a missing CTA→V2 link, because that link is not in production.

## LIMITI_RESIDUI

FOT:

- Executor uses `swapExactTokensForTokens` / `swapExactETHForTokens` / `swapExactTokensForETH` only. No `*SupportingFeeOnTransferTokens`.
- `MELEGA_DEX_FOT_UNPROVEN` is thrown only when a caller sets `feeOnTransferUnproven: true`. Public preflight does **not** invent FOT detection. Unused error name ≠ FOT protection.

Exact-out:

- Executor is exact-in only.
- Binding: `V2_BINDING_EXACT_OUT_UNSUPPORTED`.
- Preflight: `EXACT_OUT` → V2 unavailable. Legacy exact-out is unchanged.

Paths:

- Binding path comes from the winner quote hops + certified wrapped native. No multi-hop invention from symbols.
- Melega without a matching snapshot path is input→output only.
- Cross-chain and split routes remain forbidden in SHADOW competition.

Other:

- No real-router fork execute proof.
- No public executor.
- SHADOW timeout 8s; failure leaves legacy Swap intact (preflight isolation).

## AZIONI_FOUNDER_ESATTE

Review-only. This agent stops here.

1. Read this report and `deployments/smartswap-executor-v2/*`.
2. Treat public Swap as **legacy only**. Do not announce V2 go-live.
3. Do not broadcast constructor / `setRouter` / `execute` from this package. Data is NOT-EXECUTED.
4. Do not paste a guessed executor address into the app.
5. A later deploy decision (separate authorization) would be: per-chain `constructor(treasury, wrappedNative, owner)`, then the three `setRouter` rows above, then a **separate** runtime mission to connect CTA → `prepareV2UserTransactions`. Until that mission exists, deploy ≠ public V2.
6. Keep Treasury / owner / fee bands unchanged.
7. Do not start `/list` or Boost work from this PR.

## Verdict

**READY_FOR_FOUNDER_DEPLOYMENT_REVIEW**

**NOT GO_LIVE_READY.** Complete public path is missing (no deployed executor, no CTA→V2 link, no real-router fork execute).

Founder may review the current-main bytecode and local TS-calldata Anvil proof. Founder may not treat this as authorization to activate production V2.
