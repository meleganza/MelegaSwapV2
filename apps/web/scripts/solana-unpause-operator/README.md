# MARCO Solana unpause — Founder Mac helper

This page is static. It does **not** run on the cloud agent. Serve it on the Mac that has Phantom/Solflare.

From the MelegaSwapV2 repo root:

```
python3 -m http.server 8787 --directory apps/web/scripts/solana-unpause-operator
```

Then open http://127.0.0.1:8787/

File: `apps/web/scripts/solana-unpause-operator/index.html`
