import FounderWrapperDeployPage from 'views/TestnetWrapperDeploy/FounderWrapperDeployPage'

/** R746B — founder-only browser wrapper deploy (barePage, no registry switch) */
const TestnetWrapperDeployPage = () => <FounderWrapperDeployPage />

TestnetWrapperDeployPage.barePage = true

export default TestnetWrapperDeployPage
